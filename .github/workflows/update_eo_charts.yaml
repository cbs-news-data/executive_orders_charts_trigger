name: Refresh DW EO Charts

on:
  push:
    branches:
      - main
  schedule:
<<<<<<< HEAD
    - cron: "0 0 * * *" # Runs once a day at midnight (UTC)
  workflow_dispatch:
=======
#    - cron: '0 14,19,00 * * *'   # 9am, 2pm, 7pm ET (14:00 UTC)
  workflow_dispatch:  # Manual trigger too for remote
>>>>>>> 97d0ea77f242cfb412b631aba9f9be1144549536

jobs:
  run-scripts:
    runs-on: ubuntu-latest
    steps:
<<<<<<< HEAD
      - name: Set up R
        uses: r-lib/actions/setup-r@v2

      - name: Install packages
        uses: r-lib/actions/setup-r-dependencies@v2
        with:
          packages: |
            any::janitor

      - name: Set up environment variables based on DATAWRAPPR secret
        run: echo "DW_API_KEY=${{ secrets.DATAWRAPPER_API_KEY }}" >> $GITHUB_ENV

      - name: Install DatawRappr package
        run: |
          Rscript -e "install.packages('pak')"
          Rscript -e "pak::pkg_install('munichrocker/DatawRappr')"

      - name: Check out repository
        uses: actions/checkout@v4

      - name: Run script
        run: Rscript -e 'source("update_charts.R")'
=======
    - uses: actions/checkout@v3
    - uses: r-lib/actions/setup-r@v2
      with:
        r-version: '4.1.0'  # Specify a compatible R version
    - name: Install dependencies
      env:
        GITHUB_PAT: ${{ secrets.PAT_GITHUB }}
      run: |
        Rscript -e 'install.packages("remotes")'
        Rscript -e 'remotes::install_github("munichrocker/DatawRappr", auth_token = Sys.getenv("GITHUB_PAT"))'
    - name: Run R script
      env:
        DATAWRAPPER_API_KEY: ${{ secrets.DATAWRAPPER_API_KEY }}
      run: Rscript update_charts.R
>>>>>>> 97d0ea77f242cfb412b631aba9f9be1144549536
